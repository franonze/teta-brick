const CONFIG = {
    app: {
        mergeWindowMinutes: 60,
        minSecondsToKeepTimer: 10,
        defaultNextFeedingHours: 4
    },
    alarm: {
        frequencyHz: 800,
        repeatIntervalMs: 2000
    },
    storage: {
        historyKey: 'babyLogHistory',
        stateKey: 'babyLogCurrentState'
    }
};
